<<<<<<< Updated upstream
# 파이썬
__pycache__/
*.pyc

# 실행 산출물 — 돌릴 때마다 다시 생기고, 스크린샷은 실행당 수 MB다.
# 지도(maps/)와 페르소나(personas/)는 입력이라 저장한다.
agent-ux/logs/
agent-ux/shots/
agent-ux/shots-real*/
agent-ux/maps/*.bak

# 키는 환경변수로만 다룬다. 파일로 들어올 자리를 아예 막는다.
.env
*.key
secrets*

# 측정·촬영 산출물 — 돌릴 때마다 다시 생긴다
agent-ux/shots_*/
agent-ux/measured_*.csv
agent-ux/*.bak
agent-ux/maps/*.4pages.json

# 확인용으로 찍은 스크린샷 — 디버깅 흔적이지 산출물이 아니다
agent-ux/*_check*.png
agent-ux/web_*.png
web/node_modules/
web/dist/

# 서버가 답사 때 찍어 쌓는 스크린샷 — 실행할 때마다 다시 생긴다
agent-ux/shots_web/
web/.env
=======
node_modules/
dist/
.claude/settings.local.json
>>>>>>> Stashed changes

# 확인용으로 찍은 화면. 결과물이 아니라 작업 중 눈으로 본 것들이다.
agent-ux/*.png
testbed-comparison.html

# 페르소나 규격을 바꾸기 전에 뜬 사본. 되돌릴 일이 있으면 git 에서 꺼낸다.
agent-ux/personas/*.bak

# 로컬에서만 쓰는 실행 스위치. 커밋하면 배포 빌드도 이 값을 읽어 데모가 깨진다.
web/.env.local
